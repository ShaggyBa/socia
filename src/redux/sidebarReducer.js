import friendsData from "../data/sidebarData/friends";

const sidebarDefault = {
    friendsData //Данные о друзьях
}

const sidebarReducer = (state = sidebarDefault, action) => {


    return state
}

export default sidebarReducer